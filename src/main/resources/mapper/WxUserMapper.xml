<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safziy.mapper.WxUserMapper">

    <select id="findByOpenId" resultType="com.safziy.entity.WxUser">
        select * from `t_wx_user` where `open_id` = #{openId}
    </select>

</mapper>
